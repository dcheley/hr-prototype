<div class="background">
  <% if current_user == @user %>
    <h1 class="form-title">Edit Account Settings</h1>

    <div class="container">
      <%= form_for(@user) do |f| %>
        <% if @user.errors.any? %>
          <h2 class="error-header"><%= pluralize(@user.errors.count, "error") %> occurred!</h2>

          <ul class="error-list">
            <% @user.errors.full_messages.each do |msg| %>
              <li class="error-msg"><%= msg %></li>
            <% end %>
          </ul>

        <% end %>

        <div class="field">
          <%= f.label :email, class:"field-label" %>
          <%= f.text_field :email, class:"field-input" %>
        </div>

        <div class="field">
          <%= f.label :name, class:"field-label" %>
          <%= f.text_field :name, class:"field-input" %>
        </div>

        <div class="field">
          <%= f.label :title, class:"field-label" %>
          <%= f.text_field :title, class:"field-input" %>
        </div>

        <div class="field">
          <%= f.label :feedback, class:"field-label" %>
          <%= f.text_area :feedback, class:"field-input", placeholder:"What sort of progress were you hoping to achieve by signing up for this service?" %>
        </div>

        <div class="avatar_container">
          <%= f.file_field :avatar %>
        </div>

        <div class="divider"></div>

        <h5 style="padding-top: 15px;">Please check the relevant managerial level</h5>

        <div class="check-box">
          <%= f.check_box :adm %>
          <%= f.label :adm%>
        </div>

        <div class="check-box">
          <%= f.check_box :director %>
          <%= f.label :director %>
        </div>

        <div class="check-box">
          <%= f.check_box :manager %>
          <%= f.label :manager %>
        </div>

        <div class="check-box">
          <%= f.check_box :staff %>
          <%= f.label :staff %>
        </div>

        <div class="divider"></div>

        <h5 style="padding-top: 15px;">Please check the relevant branch</h5>

        <div class="check-box">
          <%= f.check_box :adm_office%>
          <%= f.label :adm_office %>
        </div>

        <div class="check-box">
          <%= f.check_box :strategic_human_resources %>
          <%= f.label :strategic_human_resources %>
        </div>

        <div class="check-box">
          <%= f.check_box :service_management_and_facilities %>
          <%= f.label :service_management_and_facilities %>
        </div>

        <div class="check-box">
          <%= f.check_box :business_planning_and_finance %>
          <%= f.label :business_planning_and_finance %>
        </div>

        <%= f.submit "SUBMIT", class:"submit" %>

      <% end %>
    </div>
  <% else %>
    <h1 class="login-message"><%= link_to "LOGIN", :login %> to edit profile</h1>
  <% end %>
</div>
