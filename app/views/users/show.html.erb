<div class="background", style="height: 800px;">
  <% if current_user && current_user == @user %>
    <!-- <h1 class="profile-title"><%= link_to "Edit Settings", "/users/#{@user.id}/edit" %></h1> -->
    </br><h1 class="profile-title">Your Profile</h1>

    <% if @user.avatar.file? == true %>
      <%= image_tag @user.avatar.url(:medium), class:"profile-pic" %>
    <% else %>
      <div class="default-pic">
        <%= image_tag('smile.svg') %>
      </div>
      <!-- <li class="pic-info">Add a profile pic through the settings page (link at top)</li> -->
    <% end %>

    <ul style="padding-left: 0;">
      <% if @user.adm_office == true %>
        <li class="profile-info"><%= link_to "TEAM", "/users/admo_org_chart", style:"color: white; font-weight: bold;"%></li>
      <% elsif @user.strategic_human_resources == true %>
        <li class="profile-info"><%= link_to "TEAM", "/users/hr_org_chart", style:"color: white; font-weight: bold;"%></li>
      <% elsif @user.service_management_and_facilities == true %>
        <li class="profile-info"><%= link_to "TEAM", "/users/smf_org_chart", style:"color: white; font-weight: bold;"%></li>
      <% elsif @user.business_planning_and_finance == true %>
        <li class="profile-info"><%= link_to "TEAM", "/users/bpf_org_chart", style:"color: white; font-weight: bold;"%></li>
      <% end %>
      <li class="profile-info"><b>Title:</b> <%= @user.title %></li>
      <li class="profile-info"><b>Email:</b> <%= @user.email %></li>
      <li class="profile-info"><b>Phone:</b> <%= @user.phone %></li>
    </ul>

    </br><div class="badge-container">
      <h2 class="badge-title">Your badges (<b>click on</b> an individual <b>badge</b> to <b>edit</b> details):</h2>

      <p class="profile-link"><%= link_to 'ADD Badge', "/users/#{@user.id}/badges/new" %></p>

      <ul>
        <!-- Display Red Badges -->
        <% @user.badges.where(red: true).each do |b| %>
        <div class="badge-red">
          <li class="profile-badges">
            <%= link_to "#{b.name}", "/users/#{@user.id}/badges/#{b.id}/edit" %>
          </li></br>
          <!-- <%= button_to 'DELETE', {:controller => 'badges', :action => 'destroy', :user_id => current_user.id, :id => "#{b.id}"}, method: :delete, data: { confirm: "You sure? Badge will be permanenetly removed!" }, class:"btn-default" %> -->
        </div>
        <% end %>
        <!-- Display Blue Badges -->
        <% @user.badges.where(blue: true).each do |b| %>
        <div class="badge-blue">
          <li class="profile-badges">
            <%= link_to "#{b.name}", "/users/#{@user.id}/badges/#{b.id}/edit" %>
          </li></br>
          <!-- <%= button_to 'DELETE', {:controller => 'badges', :action => 'destroy', :user_id => current_user.id, :id => "#{b.id}"}, method: :delete, data: { confirm: "You sure? Badge will be permanenetly removed!" }, class:"btn-default" %> -->
        </div>
        <% end %>
        <!-- Display Yellow Badges -->
        <% @user.badges.where(yellow: true).each do |b| %>
        <div class="badge-yellow">
          <li class="profile-badges">
            <%= link_to "#{b.name}", "/users/#{@user.id}/badges/#{b.id}/edit" %>
          </li></br>
          <!-- <%= button_to 'DELETE', {:controller => 'badges', :action => 'destroy', :user_id => current_user.id, :id => "#{b.id}"}, method: :delete, data: { confirm: "You sure? Badge will be permanenetly removed!" }, class:"btn-default" %> -->
        </div>
        <% end %>
        <!-- Display Green Badges -->
        <% @user.badges.where(green: true).each do |b| %>
        <div class="badge-green">
          <li class="profile-badges">
            <%= link_to "#{b.name}", "/users/#{@user.id}/badges/#{b.id}/edit" %>
          </li></br>
          <!-- <%= button_to 'DELETE', {:controller => 'badges', :action => 'destroy', :user_id => current_user.id, :id => "#{b.id}"}, method: :delete, data: { confirm: "You sure? Badge will be permanenetly removed!" }, class:"btn-default" %> -->
        </div>
        <% end %>
        <!-- Display Purple Badges -->
        <% @user.badges.where(purple: true).each do |b| %>
        <div class="badge-purple">
          <li class="profile-badges">
            <%= link_to "#{b.name}", "/users/#{@user.id}/badges/#{b.id}/edit" %>
          </li></br>
          <!-- <%= button_to 'DELETE', {:controller => 'badges', :action => 'destroy', :user_id => current_user.id, :id => "#{b.id}"}, method: :delete, data: { confirm: "You sure? Badge will be permanenetly removed!" }, class:"btn-default" %> -->
        </div>
        <% end %>
        <!-- Display Pink Badges -->
        <% @user.badges.where(pink: true).each do |b| %>
        <div class="badge-pink">
          <li class="profile-badges">
            <%= link_to "#{b.name}", "/users/#{@user.id}/badges/#{b.id}/edit" %>
          </li></br>
          <!-- <%= button_to 'DELETE', {:controller => 'badges', :action => 'destroy', :user_id => current_user.id, :id => "#{b.id}"}, method: :delete, data: { confirm: "You sure? Badge will be permanenetly removed!" }, class:"btn-default" %> -->
        </div>
        <% end %>
        <!-- Display Grey Badges -->
        <% @user.badges.where(grey: true).each do |b| %>
        <div class="badge-grey">
          <li class="profile-badges">
            <%= link_to "#{b.name}", "/users/#{@user.id}/badges/#{b.id}/edit", style:"color: black;" %>
          </li></br>
          <!-- <%= button_to 'DELETE', {:controller => 'badges', :action => 'destroy', :user_id => current_user.id, :id => "#{b.id}"}, method: :delete, data: { confirm: "You sure? Badge will be permanenetly removed!" }, class:"btn-default" %> -->
        </div>
        <% end %>
      </ul>
    </div>

  <% elsif current_user && current_user != @user %>
    </br><h1 class="profile-title"><%= @user.name %>'s Profile</h1>

    <% if @user.avatar.file? == true %>
      <%= image_tag @user.avatar.url(:medium), class:"profile-pic" %>
    <% else %>
      <div class="default-pic">
        <%= image_tag('smile.svg') %>
      </div>
      <li class="pic-info">Add a profile pic through the settings page (link at top)</li>
    <% end %>

    <ul style="padding-left: 0;">
      <% if @user.adm_office == true %>
        <li class="profile-info"><%= link_to "TEAM", "/users/admo_org_chart", style:"color: white; font-weight: bold;"%></li>
      <% elsif @user.strategic_human_resources == true %>
        <li class="profile-info"><%= link_to "TEAM", "/users/hr_org_chart", style:"color: white; font-weight: bold;"%></li>
      <% elsif @user.service_management_and_facilities == true %>
        <li class="profile-info"><%= link_to "TEAM", "/users/smf_org_chart", style:"color: white; font-weight: bold;"%></li>
      <% elsif @user.business_planning_and_finance == true %>
        <li class="profile-info"><%= link_to "TEAM", "/users/bpf_org_chart", style:"color: white; font-weight: bold;"%></li>
      <% end %>
      <li class="profile-info"><b>Title:</b> <%= @user.title %></li>
      <li class="profile-info"><b>Email:</b> <%= @user.email %></li>
      <li class="profile-info"><b>Phone:</b> <%= @user.phone %></li>
    </ul>

    </br><div class="badge-container">
      <h2 class="badge-title"><%= "#{@user.name}" %>'s badges: Click a badge to learn more about their skills and experience.</h2>

      <p class="profile-link"><%= link_to 'GIVE Badge', "/users/#{@user.id}/badges/new" %></p>

      <ul>
        <!-- Display Red Badges -->
        <% @user.badges.where(red: true).each do |b| %>
        <div class="badge-red">
          <li class="profile-badges">
            <%= link_to "#{b.name}", "/users/#{@user.id}/badges/#{b.id}" %>
          </li></br>
        </div>
        <% end %>
        <!-- Display Blue Badges -->
        <% @user.badges.where(blue: true).each do |b| %>
        <div class="badge-blue">
          <li class="profile-badges">
            <%= link_to "#{b.name}", "/users/#{@user.id}/badges/#{b.id}" %>
          </li></br>
        </div>
        <% end %>
        <!-- Display Yellow Badges -->
        <% @user.badges.where(yellow: true).each do |b| %>
        <div class="badge-yellow">
          <li class="profile-badges">
            <%= link_to "#{b.name}", "/users/#{@user.id}/badges/#{b.id}" %>
          </li></br>
        </div>
        <% end %>
        <!-- Display Green Badges -->
        <% @user.badges.where(green: true).each do |b| %>
        <div class="badge-green">
          <li class="profile-badges">
            <%= link_to "#{b.name}", "/users/#{@user.id}/badges/#{b.id}" %>
          </li></br>
        </div>
        <% end %>
        <!-- Display Purple Badges -->
        <% @user.badges.where(purple: true).each do |b| %>
        <div class="badge-purple">
          <li class="profile-badges">
            <%= link_to "#{b.name}", "/users/#{@user.id}/badges/#{b.id}" %>
          </li></br>
        </div>
        <% end %>
        <!-- Display Pink Badges -->
        <% @user.badges.where(pink: true).each do |b| %>
        <div class="badge-pink">
          <li class="profile-badges">
            <%= link_to "#{b.name}", "/users/#{@user.id}/badges/#{b.id}" %>
          </li></br>
        </div>
        <% end %>
        <!-- Display Grey Badges -->
        <% @user.badges.where(grey: true).each do |b| %>
        <div class="badge-grey">
          <li class="profile-badges">
            <%= link_to "#{b.name}", "/users/#{@user.id}/badges/#{b.id}", style:"color: black;" %>
          </li></br>
        </div>
        <% end %>
      </ul>
    </div>
    <% else %>
      <h1 class="login-message"><%= link_to "LOGIN", :login %> to view profiles</h1>
    <% end %>
</div>
