<% if params[:search] %>
  <%= @users.count %> Profiles found:
  <% @users.each do |u| %>
    <%= u.name %>
  <% end %>
<% end %>

<% if @users.blank? %>
  <% flash[:alert] = "There are currently no profiles containing the #{params[:search]} badge." %>
<% end %>
