<ul class="nav nav-pills nav-justified">
  <li role="presentation" class="dropdown active">
    <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
      Bucket List<span class="caret"></span>
    </a>
    <ul class="dropdown-menu">
      <a class="tab-title" href="#bucket" data-toggle="pill">Badges</a>
      <p class="badge-list-title"><%= link_to "View Bucket List", "/users/opps", class:"team-link" %></p>
    </ul>
  </li>

  <li role="presentation" class="dropdown">
    <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
      Recognition<span class="caret"></span>
    </a>
    <ul class="dropdown-menu">
      <a class="tab-title" href="#bucket" data-toggle="pill">Badges</a>
      <p class="badge-list-title"><%= link_to "Recognize colleagues", "/users/org_charts", class:"team-link" %></p>
    </ul>
  </li>
</ul>

<div class="tab-content">
  <div role="tabpane1" class="tab-pane active" id="bucket">
    <div class="badge-row">        <!-- Display Green Badges -->
      <% @user.badges.where.not(opportunity_id: nil).each do |b| %>
        <%= link_to "/users/#{@user.id}/badges/#{b.id}" do %>
          <div class="badge-green">
            <li class="profile-badges-name">
              <%= "#{b.name}" %>
            </li></br>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
  <div role="tabpane1" class="tab-pane" id="recognitions">
    <div class="badge-row">
      <!-- Display Green Badges -->
      <% @user.received_badges.each do |b| %>
        <%= link_to "/users/#{@user.id}/recognitions/#{b.id}" do %>
          <div class="badge-green">
            <li class="profile-badges-name">
              <%= "#{b.name}" %>
            </li></br>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
