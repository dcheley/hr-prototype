<div class="row">
  <h4 class="profile-title">Bucket List</h4>
  <p class=""><%= link_to "View Bucket List", "/users/opps", class:"team-link" %></p>

  <!-- Display Exp/skills Badges -->
  <% @user.badges.where.not(opportunity_id: nil).each do |b| %>
    <%= link_to "/users/#{@user.id}/badges/#{b.id}" do %>
      <div class="badge-green">
        <li class="profile-badges-name">
          <%= "#{b.name}" %>
        </li></br>
      </div>
    <% end %>
  <% end %>
</div>

<div class="row">
  <h4 class="profile-title">Recognition</h4>
  <p class=""><%= link_to "Recognize colleagues", "/users/org_charts", class:"team-link" %></p>

  <!-- Display Recognition Badges -->
  <% @user.received_badges.each do |b| %>
    <%= link_to "/users/#{@user.id}/recognitions/#{b.id}" do %>
      <div class="badge-green">
        <li class="profile-badges-name">
          <%= "#{b.name}" %>
        </li></br>
      </div>
    <% end %>
  <% end %>
</div>
